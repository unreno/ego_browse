<h1>Editing Eligibility Screening</h1>

<%= render 'form', eligibility_screening: @eligibility_screening %>

<% if current_user.present? and current_user.can_read? %>
<%= link_to 'Show', @eligibility_screening %> |
<%= link_to 'Back', eligibility_screenings_path %>
<% end %>
