<h1>Editing Alter Referral Sheet</h1>

<%= render 'form', alter_referral_sheet: @alter_referral_sheet %>

<% if current_user.present? and current_user.can_read? %>
<%= link_to 'Show', @alter_referral_sheet %> |
<%= link_to 'Back', alter_referral_sheets_path %>
<% end %>
