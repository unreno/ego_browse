<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
	<thead>
		<tr>
			<th>&nbsp;</th>
			<th>Email</th>
			<th>Password</th>
			<th>Name</th>
			<th>Lastactivity</th>
			<th>Permissions</th>
		</tr>
	</thead>

	<tbody>
		<% @users.each do |user| %>
			<tr>
				<td><%= link_to 'Show', user %></td>
				<td><%= user.email %></td>
				<td><%= user.password %></td>
				<td><%= user.name %></td>
				<td><%= user.lastActivity %></td>
				<td><%= user.permissions %></td>
			</tr>
		<% end %>
	</tbody>
</table>
