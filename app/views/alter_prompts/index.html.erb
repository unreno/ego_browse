<p id="notice"><%= notice %></p>

<h1>Alter Prompts</h1>

<table>
  <thead>
    <tr>
      <th>Studyid</th>
      <th>Afteraltersentered</th>
      <th>Display</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @alter_prompts.each do |alter_prompt| %>
      <tr>
        <td><%= alter_prompt.studyId %></td>
        <td><%= alter_prompt.afterAltersEntered %></td>
        <td><%= alter_prompt.display %></td>
        <td><%= link_to 'Show', alter_prompt %></td>
      </tr>
    <% end %>
  </tbody>
</table>
